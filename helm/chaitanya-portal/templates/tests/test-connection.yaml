apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "chaitanya-portal.fullname" . }}-test-connection"
  labels:
    {{- include "chaitanya-portal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "chaitanya-portal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
