apiVersion: v1
kind: Service
metadata:
  name: {{ include "security-service.fullname" . }}
  labels:
    {{- include "security-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "security-service.selectorLabels" . | nindent 4 }}
